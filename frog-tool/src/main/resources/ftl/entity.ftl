package ${packageName};

import com.me.metadata.db.BaseEntity;

import java.util.Objects;

/**
 * @author Generated by database tools,  DO NOT EDIT!!!
 */
public class ${className} extends BaseEntity {

<#if filedList?exists>
    <#list filedList as filed>
    /**
     * ${filed.comment!}
     */
    private ${filed.toJavaType()} ${filed.name};

    </#list>
</#if>

<#if filedList?exists>
    <#list filedList as filed>
    /**
     * ${filed.comment!}
     */
    public void set${filed.name?cap_first}(${filed.toJavaType()} ${filed.name}) {
        if (Objects.equals(this.${filed.name}, ${filed.name})) {
            return;
        }
        this.${filed.name} = ${filed.name};
        this.setUpdate(true);
    }

    /**
     * ${filed.comment!}
     */
    public ${filed.toJavaType()} get${filed.name?cap_first}() {
        return this.${filed.name};
    }

    </#list>
</#if>
    @Override
    public ${className} clone() {
        ${className} entity = new ${className}();
        <#list filedList as filed>
        entity.${filed.name} = this.${filed.name};
        </#list>
        return entity;
    }
}
